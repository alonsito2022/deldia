<!-- res/drawable/search_view_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo -->
    <solid android:color="@color/white" /> <!-- Cambia el color según tu diseño -->

    <!-- Bordes redondeados -->
    <corners android:radius="16dp" /> <!-- Ajusta el radio para bordes más o menos redondeados -->

    <!-- Borde opcional -->
    <stroke
        android:width="1dp"
        android:color="@color/primary" /> <!-- Color y grosor del borde -->
</shape>